server:
  port: 8080

spring:
  application:
    name: eventos-demo
  # Desactivar RabbitMQ para desarrollo sin servidor
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    template:
      retry:
        enabled: false

logging:
  level:
    com.example.eventos: DEBUG
